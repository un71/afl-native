import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Профиль</Text>
      </View>
    </SafeAreaView>
  );
};
